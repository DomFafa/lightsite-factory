You are repairing a generated static tool site inside the current run folder.

Follow the QA failures exactly. Do not change the product direction, remove required calculator functionality, weaken QA standards, or add external services.

For manual visual tuning:
- Do not automatically require or run pnpm qa after a human-directed visual edit.
- Run pnpm qa only when the user explicitly asks for QA.
- Prefer HTML/CSS edits for visual polish; do not rewrite JavaScript calculation logic unless the defect is clearly functional.
