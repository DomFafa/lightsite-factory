Generate a high-fidelity first-screen web UI mockup for the tool described in the input.

The image must look like a real polished static tool website, not a generic AI-coded form.

Requirements:
- Tool-first first viewport with visible real controls and a realistic result state.
- Match the provided tool_family and tool_spec.
- Use compact UI tokens for chips, status pills, and section labels.
- H1 should be strong but not oversized.
- Decorative illustration is allowed, but it must not overpower the tool.
- Primary tool controls/results must remain the visual center.
- Do not include irrelevant 401k, finance, retirement, salary, employer match, or tax content unless is401k is true.
- Do not include backend, login, account, or database UI.
- For random date generator, avoid oversized cartoon-like badges; use a refined playful utility style.
- For complex tools, design only a conservative V1 scope.
- Use readable, production-quality spacing and hierarchy.

Return only the image. Do not create code.
